openapi: 3.0.0

info:
  title: 'Pinnacle API'
  description: 'The complete API for the Pinnacle platform.'
  version: '1.0.0'

servers:
  - url: 'http://localhost:3000'
    description: 'Local Development Server'

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    id:
      name: id
      in: path
      required: true
      description: 'The unique identifier of the resource.'
      schema:
        type: string
    jobId:
      name: jobId
      in: path
      required: true
      description: 'The unique identifier of the job.'
      schema:
        type: string
    applicationId:
      name: applicationId
      in: path
      required: true
      description: 'The unique identifier of the application.'
      schema:
        type: string

  schemas:
    Announcement:
      type: object
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        content:
          type: string
        createdAt:
          type: string
          format: date-time
        sender:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
            email:
              type: string
    Accomplishment:
      type: object
      properties:
        id: { type: string, format: uuid }
        type: { type: string }
        title: { type: string }
        issuer: { type: string }
        date: { type: string }
        description: { type: string }
        url: { type: string }
        order: { type: integer }
    PositionOfResponsibility:
      type: object
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
        organization: { type: string }
        startDate: { type: string }
        endDate: { type: string }
        current: { type: boolean }
        description: { type: string }
        order: { type: integer }
    JobEligibility:
      type: object
      properties:
        id:
          type: string
          format: uuid
        jobId:
          type: string
          format: uuid
        minCgpa:
          type: number
          nullable: true
          minimum: 0
          maximum: 10
        maxActiveBacklogs:
          type: integer
          nullable: true
          minimum: 0
        maxTotalBacklogs:
          type: integer
          nullable: true
          minimum: 0
        allowedBranches:
          type: array
          items:
            type: string
          default: []
        allowedYears:
          type: array
          items:
            type: integer
            minimum: 1
            maximum: 5
          default: []
        customCriteria:
          type: object
          nullable: true
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    UserDetails:
      type: object
      properties:
        id: { type: string, format: uuid }
        userId: { type: string, format: uuid }
        name: { type: string }
        branch: { type: string }
        address: { type: string }
        parentName: { type: string }
        parentMobileNumber: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateUserDetailsRequest:
      type: object
      required: [name, branch, address, parentName, parentMobileNumber]
      properties:
        name: { type: string }
        branch: { type: string }
        address: { type: string }
        parentName: { type: string }
        parentMobileNumber: { type: string }

security:
  - bearerAuth: []

# API Endpoints
paths:
  # System
  /health:
    get:
      summary: 'Health Check'
      tags: [System]
      security: [] # Public endpoint
      responses: { '200': { description: 'Service is healthy.' } }

  # Auth
  /auth/google/login:
    get:
      {
        summary: 'Initiate Google OAuth Login',
        tags: [Auth],
        security: [],
        responses: { '200': { description: 'Success' } },
      }
  /auth/google/callback:
    get:
      {
        summary: 'Google OAuth Callback',
        tags: [Auth],
        security: [],
        responses: { '200': { description: 'Success' } },
      }
  /auth/refresh:
    post:
      {
        summary: 'Refresh Access Token',
        tags: [Auth],
        security: [],
        responses: { '200': { description: 'Success' } },
      }
  /auth/me:
    get:
      {
        summary: 'Get Current Authenticated User',
        tags: [Auth],
        responses: { '200': { description: 'Success' } },
      }

  # User Details (Student Profile)
  /api/user-details:
    get:
      summary: 'Get user personal details'
      tags: [UserDetails]
      responses:
        '200':
          description: 'User details retrieved successfully'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserDetails'
        '404':
          description: 'User details not found'
    put:
      summary: 'Add user personal details'
      tags: [UserDetails]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserDetailsRequest'
      responses:
        '201':
          description: 'User details added successfully'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserDetails'
        '403':
          description: 'User has already onboarded'

  /api/user-details/profile:
    get:
      {
        summary: "Get user's core profile",
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
    patch:
      {
        summary: "Update user's core profile",
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/experiences:
    get:
      {
        summary: 'Get all experiences',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
    post:
      {
        summary: 'Create an experience',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/experiences/{id}:
    patch:
      {
        summary: 'Update an experience',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
    delete:
      {
        summary: 'Delete an experience',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/education:
    get:
      {
        summary: 'Get all education entries',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
    post:
      {
        summary: 'Create an education entry',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/education/{id}:
    patch:
      {
        summary: 'Update an education entry',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
    delete:
      {
        summary: 'Delete an education entry',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/skills:
    get:
      {
        summary: 'Get all skills',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
    post:
      {
        summary: 'Create a skill entry',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/skills/{id}:
    patch:
      {
        summary: 'Update a skill entry',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
    delete:
      {
        summary: 'Delete a skill entry',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/projects:
    get:
      {
        summary: 'Get all projects',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
    post:
      {
        summary: 'Create a project entry',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/projects/{id}:
    patch:
      {
        summary: 'Update a project entry',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
    delete:
      {
        summary: 'Delete a project entry',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/certifications:
    get:
      {
        summary: 'Get all certifications',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
    post:
      {
        summary: 'Create a certification entry',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/certifications/{id}:
    patch:
      {
        summary: 'Update a certification entry',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
    delete:
      {
        summary: 'Delete a certification entry',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/accomplishments:
    get:
      {
        summary: 'Get all accomplishments',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
    post:
      {
        summary: 'Create an accomplishment',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/accomplishments/{id}:
    patch:
      {
        summary: 'Update an accomplishment',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
    delete:
      {
        summary: 'Delete an accomplishment',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/extracurriculars:
    get:
      {
        summary: 'Get all positions of responsibility',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
    post:
      {
        summary: 'Create a position of responsibility',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/extracurriculars/{id}:
    patch:
      {
        summary: 'Update a position of responsibility',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
    delete:
      {
        summary: 'Delete a position of responsibility',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/languages:
    get:
      {
        summary: 'Get all languages',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
    post:
      {
        summary: 'Create a language entry',
        tags: [UserDetails],
        responses: { '200': { description: 'Success' } },
      }
  /api/user-details/languages/{id}:
    patch:
      {
        summary: 'Update a language entry',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
    delete:
      {
        summary: 'Delete a language entry',
        tags: [UserDetails],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }

  # Company & Job Management (Admin)
  /api/companies:
    get:
      {
        summary: 'Get all companies',
        tags: [Company],
        responses: { '200': { description: 'Success' } },
      }
    post:
      {
        summary: 'Create a company (Admin Only)',
        tags: [Company],
        responses: { '200': { description: 'Success' } },
      }
  /api/companies/{id}:
    get:
      {
        summary: 'Get company by ID',
        tags: [Company],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
    patch:
      {
        summary: 'Update a company (Admin Only)',
        tags: [Company],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
    delete:
      {
        summary: 'Delete a company (Admin Only)',
        tags: [Company],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
  /api/companies/search:
    get:
      summary: 'Search for companies'
      tags: [Company]
      parameters:
        - name: q
          in: query
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 'Success'

  # Jobs Management
  /api/jobs:
    get:
      summary: 'Get all available jobs'
      description: 'Public endpoint - returns all open job postings'
      tags: [Jobs]
      security: [] # Public endpoint
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: q
          in: query
          description: Search by job title or company name
          schema:
            type: string
        - name: industry
          in: query
          description: Filter by company industry
          schema:
            type: string
        - name: jobType
          in: query
          description: Filter by job type
          schema:
            type: string
        - name: companyId
          in: query
          description: Filter by company ID
          schema:
            type: string
      responses:
        '200':
          description: 'Success'
    post:
      summary: 'Create a new job posting'
      description: 'Admin only - create a new job posting'
      tags: [Jobs]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [placementCycleId, companyId, title]
              properties:
                placementCycleId: { type: string, format: uuid }
                companyId: { type: string, format: uuid }
                title: { type: string }
                description: { type: string }
                descriptionDocument: { type: string }
                location: { type: string }
                type: { type: string }
                salary: { type: string }
                deadline: { type: string, format: date-time }
                questions:
                  type: array
                  items:
                    type: object
                    properties:
                      question: { type: string }
                      required: { type: boolean }
      responses:
        '201':
          description: 'Job created successfully'
  /api/jobs/{id}:
    get:
      summary: 'Get a single job by ID'
      description: 'Public endpoint - get details of a specific job'
      tags: [Jobs]
      security: [] # Public endpoint
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: 'Success'
  /api/jobs/{id}/close:
    patch:
      summary: 'Close a job posting'
      description: 'Admin only - close/deactivate a job posting'
      tags: [Jobs]
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: 'Success'
  /api/jobs/{jobId}/applications:
    post:
      summary: 'Apply to a job'
      description: 'Authenticated users can submit an application to a job'
      tags: [Jobs]
      parameters: [{ $ref: '#/components/parameters/jobId' }]
      responses:
        '201':
          description: 'Application submitted successfully'
    get:
      summary: 'Get all applications for a job'
      description: 'Admin only - view all applicants for a specific job'
      tags: [Jobs]
      parameters: [{ $ref: '#/components/parameters/jobId' }]
      responses:
        '200':
          description: 'Success'

  # Job Eligibility
  /api/jobs/{jobId}/eligibility:
    post:
      summary: 'Create or update eligibility criteria for a job'
      description: 'Admin only - set eligibility requirements (CGPA, backlogs, branches, years)'
      tags: [Jobs, Eligibility]
      parameters: [{ $ref: '#/components/parameters/jobId' }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                minCgpa:
                  type: number
                  minimum: 0
                  maximum: 10
                  example: 7.5
                maxActiveBacklogs:
                  type: integer
                  minimum: 0
                  example: 0
                maxTotalBacklogs:
                  type: integer
                  minimum: 0
                  example: 2
                allowedBranches:
                  type: array
                  items:
                    type: string
                  example: ['Computer Science', 'Electronics and Communication']
                allowedYears:
                  type: array
                  items:
                    type: integer
                    minimum: 1
                    maximum: 5
                  example: [3, 4]
                customCriteria:
                  type: object
                  additionalProperties: true
      responses:
        '201':
          description: Eligibility criteria created/updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobEligibility'
    get:
      summary: 'Get eligibility criteria for a job'
      description: 'Get the eligibility requirements for a specific job'
      tags: [Jobs, Eligibility]
      parameters: [{ $ref: '#/components/parameters/jobId' }]
      responses:
        '200':
          description: Eligibility criteria retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobEligibility'
        '404':
          description: No eligibility criteria set for this job

  /api/jobs/{jobId}/check-eligibility:
    get:
      summary: 'Check if current user is eligible for a job'
      description: 'Returns whether the authenticated user meets the eligibility requirements'
      tags: [Jobs, Eligibility]
      parameters: [{ $ref: '#/components/parameters/jobId' }]
      responses:
        '200':
          description: Eligibility check completed
          content:
            application/json:
              schema:
                type: object
                properties:
                  isEligible:
                    type: boolean
                    example: true

  # Applications Management
  /api/applications:
    get:
      summary: 'Get my applications'
      description: 'Get all applications submitted by the current user'
      tags: [Applications]
      responses:
        '200':
          description: 'Success'
  /api/applications/{id}/status:
    patch:
      summary: "Update an application's status"
      description: 'Admin only - update the status of an application (e.g., accepted, rejected)'
      tags: [Applications]
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: 'Success'
  /api/applications/{id}/withdraw:
    post:
      summary: 'Withdraw an application'
      description: 'Users can withdraw their own application'
      tags: [Applications]
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: 'Success'

  # Resume
  /api/resume/preview:
    get:
      {
        summary: 'Get user data for resume preview',
        tags: [Resume],
        responses: { '200': { description: 'Success' } },
      }
  /api/resume/templates:
    get:
      {
        summary: 'Get available resume templates',
        tags: [Resume],
        responses: { '200': { description: 'Success' } },
      }
  /api/resume/saved:
    get:
      {
        summary: 'Get all saved resumes',
        tags: [Resume],
        responses: { '200': { description: 'Success' } },
      }
    post:
      {
        summary: 'Create a new saved resume',
        tags: [Resume],
        responses: { '200': { description: 'Success' } },
      }
  /api/resume/saved/{id}:
    get:
      {
        summary: 'Get a specific saved resume',
        tags: [Resume],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
    patch:
      {
        summary: 'Update a saved resume',
        tags: [Resume],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
    delete:
      {
        summary: 'Delete a saved resume',
        tags: [Resume],
        parameters: [{ $ref: '#/components/parameters/id' }],
        responses: { '200': { description: 'Success' } },
      }
  /api/resume/generate:
    post:
      {
        summary: 'Generate PDF for authenticated user',
        tags: [Resume],
        responses: { '200': { description: 'Success' } },
      }
  /api/resume/generate/{userId}:
    post:
      {
        summary: 'Generate PDF for a specific user (Admin Only)',
        tags: [Resume],
        parameters: [{ name: userId, in: path, required: true, schema: { type: string } }],
        responses: { '200': { description: 'Success' } },
      }

  /api/resume/download/{resumeId}:
    get:
      summary: 'Get download URL for a stored resume PDF'
      description: 'Returns a presigned URL that expires in 1 hour'
      tags: [Resume]
      parameters:
        [
          {
            name: resumeId,
            in: path,
            required: true,
            description: 'The ID of the saved resume',
            schema: { type: string },
          },
        ]
      responses:
        '200':
          description: 'Success'

  /api/resume/file-info/{resumeId}:
    get:
      summary: 'Get metadata about stored resume PDF file'
      description: 'Returns file metadata (size, mimeType, createdAt, etc.)'
      tags: [Resume]
      parameters:
        [
          {
            name: resumeId,
            in: path,
            required: true,
            description: 'The ID of the saved resume',
            schema: { type: string },
          },
        ]
      responses:
        '200':
          description: 'Success'

  /api/resume/file/{resumeId}:
    delete:
      summary: 'Delete stored resume PDF file from storage'
      description: 'Removes the PDF file from cloud storage'
      tags: [Resume]
      parameters:
        [
          {
            name: resumeId,
            in: path,
            required: true,
            description: 'The ID of the saved resume',
            schema: { type: string },
          },
        ]
      responses:
        '200':
          description: 'Success'

  # Dashboard
  /api/dashboard:
    get:
      {
        summary: 'Get all user data in one request',
        tags: [Dashboard],
        responses: { '200': { description: 'Success' } },
      }
  /api/dashboard/stats:
    get:
      {
        summary: 'Get user statistics',
        tags: [Dashboard],
        responses: { '200': { description: 'Success' } },
      }
  /api/dashboard/profile-completion:
    get:
      {
        summary: 'Get profile completion percentage',
        tags: [Dashboard],
        responses: { '200': { description: 'Success' } },
      }

  # Admin Management
  /api/admin/dashboard:
    get:
      summary: 'Get admin dashboard statistics'
      description: 'Admin only - get overall platform statistics'
      tags: [Admin]
      responses:
        '200':
          description: 'Success'

  /api/admin/jobs:
    get:
      summary: 'Get all jobs with stats (Admin)'
      description: 'Admin only - get all jobs with application statistics and filters'
      tags: [Admin]
      parameters:
        - name: page
          in: query
          schema: { type: integer, default: 1 }
        - name: limit
          in: query
          schema: { type: integer, default: 20 }
        - name: search
          in: query
          description: Search by job title
          schema: { type: string }
        - name: status
          in: query
          description: Filter by job status (OPEN, CLOSED, PAUSED)
          schema: { type: string }
        - name: companyId
          in: query
          description: Filter by company ID
          schema: { type: string }
        - name: fromDate
          in: query
          description: Filter by creation date (start)
          schema: { type: string, format: date-time }
        - name: toDate
          in: query
          description: Filter by creation date (end)
          schema: { type: string, format: date-time }
      responses:
        '200':
          description: 'Success'

  /api/admin/jobs/{id}:
    get:
      summary: 'Get job by ID (Admin)'
      description: 'Admin only - get detailed job information'
      tags: [Admin]
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: 'Success'
    patch:
      summary: 'Update job details (Admin)'
      description: 'Admin only - update job information'
      tags: [Admin]
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: 'Success'
    delete:
      summary: 'Delete job (Admin)'
      description: 'Admin only - soft delete a job posting'
      tags: [Admin]
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: 'Success'

  /api/admin/jobs/{id}/reopen:
    patch:
      summary: 'Reopen a job (Admin)'
      description: 'Admin only - reopen a closed job posting'
      tags: [Admin]
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: 'Success'

  /api/admin/jobs/{id}/export:
    get:
      summary: 'Export job applications (Admin)'
      description: 'Admin only - export all applications for a job'
      tags: [Admin]
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: 'Success'

  /api/admin/jobs/{jobId}/applications:
    get:
      summary: 'Get applications for job (Admin)'
      description: 'Admin only - get all applications for a specific job'
      tags: [Admin]
      parameters: [{ $ref: '#/components/parameters/jobId' }]
      responses:
        '200':
          description: 'Success'

  /api/admin/applications:
    get:
      summary: 'Get all applications (Admin)'
      description: 'Admin only - get all applications with filters'
      tags: [Admin]
      parameters:
        - name: page
          in: query
          schema: { type: integer, default: 1 }
        - name: limit
          in: query
          schema: { type: integer, default: 20 }
        - name: search
          in: query
          description: Search by job title (case insensitive)
          schema: { type: string }
        - name: status
          in: query
          description: Filter by application status (APPLIED, SHORTLISTED, etc.)
          schema: { type: string }
        - name: jobId
          in: query
          description: Filter by job ID
          schema: { type: string }
        - name: companyId
          in: query
          description: Filter by company ID
          schema: { type: string }
        - name: fromDate
          in: query
          description: Filter by application date (start)
          schema: { type: string, format: date-time }
        - name: toDate
          in: query
          description: Filter by application date (end)
          schema: { type: string, format: date-time }
      responses:
        '200':
          description: 'Success'

  /api/admin/applications/{id}:
    get:
      summary: 'Get application details (Admin)'
      description: 'Admin only - get detailed application information'
      tags: [Admin]
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: 'Success'
    delete:
      summary: 'Delete application (Admin)'
      description: 'Admin only - delete an application'
      tags: [Admin]
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: 'Success'

  /api/admin/applications/{id}/status:
    patch:
      summary: 'Update application status (Admin)'
      description: 'Admin only - update the status of an application'
      tags: [Admin]
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: 'Success'

  /api/admin/applications/{id}/profile:
    get:
      summary: 'Get applicant profile (Admin)'
      description: 'Admin only - get full profile of an applicant'
      tags: [Admin]
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: 'Success'

  /api/admin/applications/bulk-status:
    post:
      summary: 'Bulk update application status (Admin)'
      description: 'Admin only - update status of multiple applications at once'
      tags: [Admin]
      responses:
        '200':
          description: 'Success'

  /api/admin/verify/{itemType}/{itemId}:
    patch:
      summary: 'Verify profile item (Admin)'
      description: 'Admin only - verify any user profile item (user, experience, education, skill, project, certification). For user verification, ALL related items must be verified first.'
      tags: [Admin]
      parameters:
        - name: itemType
          in: path
          required: true
          description: 'Type of item to verify'
          schema:
            type: string
            enum:
              [
                user,
                experience,
                education,
                skill,
                project,
                certification,
                accomplishment,
                positionofresponsibility,
              ]
        - name: itemId
          in: path
          required: true
          description: 'ID of the item to verify (for user type, this is the userId)'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: boolean
                  description: 'Verification status (true for verified, false for unverified)'
      responses:
        '200':
          description: 'Item verified successfully'
        '400':
          description: 'Invalid item type, missing required fields, or prerequisites not met (for user verification)'
        '404':
          description: 'Item not found'

  # Upload / CDN
  /api/upload/profile-picture:
    post:
      summary: 'Upload user profile picture'
      description: 'Authenticated users can upload a profile picture (max 5MB, images only)'
      tags: [Upload]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: 'Profile picture uploaded successfully'
          content:
            application/json:
              schema:
                type: object
                properties:
                  url:
                    type: string
                    description: 'Presigned URL to access the uploaded image'
                  message:
                    type: string
    delete:
      summary: 'Delete user profile picture'
      description: 'Authenticated users can delete their profile picture'
      tags: [Upload]
      responses:
        '200':
          description: 'Profile picture deleted successfully'

  /api/upload/company-logo/{companyId}:
    post:
      summary: 'Upload company logo (Admin)'
      description: 'Admin only - upload a company logo (max 5MB, images only)'
      tags: [Upload]
      parameters:
        - name: companyId
          in: path
          required: true
          description: 'The ID of the company'
          schema:
            type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: 'Company logo uploaded successfully'
    delete:
      summary: 'Delete company logo (Admin)'
      description: 'Admin only - delete a company logo'
      tags: [Upload]
      parameters:
        - name: companyId
          in: path
          required: true
          description: 'The ID of the company'
          schema:
            type: string
      responses:
        '200':
          description: 'Company logo deleted successfully'

  /api/upload/job-document/{jobId}:
    post:
      summary: 'Upload job description document (Admin)'
      description: 'Admin only - upload a job description document (max 10MB, PDF/DOC/DOCX)'
      tags: [Upload]
      parameters:
        - name: jobId
          in: path
          required: true
          description: 'The ID of the job'
          schema:
            type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: 'Job document uploaded successfully'
    delete:
      summary: 'Delete job description document (Admin)'
      description: 'Admin only - delete a job description document'
      tags: [Upload]
      parameters:
        - name: jobId
          in: path
          required: true
          description: 'The ID of the job'
          schema:
            type: string
      responses:
        '200':
          description: 'Job document deleted successfully'

  # Notifications
  /api/notifications:
    get:
      summary: "Get user's notifications"
      tags: [Notifications]
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
          description: Number of notifications to return
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
          description: Number of notifications to skip
      responses:
        '200':
          description: List of notifications
  /api/notifications/mark-read:
    post:
      summary: 'Mark specific notifications as read'
      tags: [Notifications]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                notificationIds:
                  type: array
                  items:
                    type: string
              required:
                - notificationIds
      responses:
        '200':
          description: Notifications marked as read
  /api/notifications/mark-all-read:
    post:
      summary: 'Mark all notifications as read'
      tags: [Notifications]
      responses:
        '200':
          description: All notifications marked as read
  /api/notifications/device-token:
    post:
      summary: 'Register device token for push notifications'
      tags: [Notifications]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                token:
                  type: string
                platform:
                  type: string
                  enum: [ios, android, web]
              required:
                - token
      responses:
        '200':
          description: Device token registered successfully
    delete:
      summary: 'Unregister device token'
      tags: [Notifications]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                token:
                  type: string
              required:
                - token
      responses:
        '200':
          description: Device token unregistered successfully

  # Announcements
  /api/announcements:
    get:
      summary: 'Get all announcements'
      tags: [Announcements]
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
          description: Page number
        - name: limit
          in: query
          schema:
            type: integer
            default: 10
          description: Number of announcements per page
      responses:
        '200':
          description: List of announcements
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: object
                    properties:
                      announcements:
                        type: array
                        items:
                          $ref: '#/components/schemas/Announcement'
                      total:
                        type: integer
                      pages:
                        type: integer
    post:
      summary: 'Create a new announcement (Admin only)'
      tags: [Announcements]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - title
                - content
              properties:
                title:
                  type: string
                content:
                  type: string
      responses:
        '201':
          description: Announcement created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: object
                    properties:
                      announcement:
                        $ref: '#/components/schemas/Announcement'
